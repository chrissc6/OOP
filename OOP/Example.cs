using System;
using System.Collections.Generic;
using System.Text;

namespace OOP
{
    class Example
    {
        public void PrintSomething()
            //public so main can see method
            //void because youre not returning anything
            //name lol
            //empty parameters because no data need to be passed in, still needed
        {
            Console.WriteLine("Print something");
        }
    }
}
